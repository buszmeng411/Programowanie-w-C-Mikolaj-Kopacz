#include <stdio.h>

int main() {
    int numer_albumu;

    printf("Wprowadz numer albumu studenta: ");
    scanf_s("%d", &numer_albumu);
    printf("Numer albumu studenta: %d\n", numer_albumu);

    return 0;
}